name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Move to code directory
      run: cd code
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag hello:$(date +%s)
